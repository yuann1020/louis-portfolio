# 3D Models

Place `lens.glb` in this directory.

The FluidGlassHero component expects:
  /assets/3d/lens.glb  →  public/assets/3d/lens.glb

Without this file the component renders nothing (graceful fallback).
The site will not crash if the file is missing.
